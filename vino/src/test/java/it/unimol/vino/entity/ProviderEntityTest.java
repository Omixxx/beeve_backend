package it.unimol.vino.entity;

import it.unimol.vino.models.entity.Provider;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class ProviderEntityTest {
/*
    @Test
    void testProviderConstructor() {
        Provider provider = new Provider();
        assertNotNull(provider);
    }

    @Test
    void testProviderGettersAndSetters() {
        Provider provider = new Provider();

        provider.setId(1L);
        assertEquals(1L, provider.getId());

        provider.setName("Provider Name");
        assertEquals("Provider Name", provider.getName());

        provider.setPhone_number("1234567890");
        assertEquals("1234567890", provider.getPhone_number());

        provider.setEmail("email@example.com");
        assertEquals("email@example.com", provider.getEmail());

        //provider.setAddress("Provider Address");
       // assertEquals("Provider Address", provider.getAddress());

       // provider.setWebsite_url("http://www.provider.com");
       // assertEquals("http://www.provider.com", provider.getWebsite_url());
    }

 */
}

